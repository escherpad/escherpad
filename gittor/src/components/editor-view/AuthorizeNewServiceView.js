export default function AuthorizeNewServiceView(_props) {
    var {accountList, dispatch} = _props;
    return (
      <div>
        Authorize New Service View
        {accountList}
      </div>
    );
}
