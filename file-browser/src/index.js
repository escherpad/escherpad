import React from 'react';
import ReactDOM from "react-dom";

window.document.addEventListener('DOMContentLoaded', function () {
    ReactDOM.render(<div/>, window.document.querySelector('#bootstrap'));
});
